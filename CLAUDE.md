@AGENTS.md

## Claude Code

- Read `AGENTS.md` as the shared project instruction source.
- Keep this file short; put cross-agent project rules in `AGENTS.md`.
- Use `CLAUDE.local.md` for personal or machine-specific preferences. It is intentionally ignored by Git.
- For larger workflow changes, update the relevant design or plan document under `docs/superpowers/` before implementing.
- When editing Skill packaging, preserve the standard `skills/<skill-name>/SKILL.md` entrypoint and avoid Claude-only structure unless explicitly requested.
